{
    "name": "ft_transcendense",
    "version": "1.0.0",
    "scripts": {
      "build": "npx esbuild src/script.ts --bundle --outfile=./public/js/bundle.js --minify",
      "hot-reload": "nodemon --watch src/ --exec 'npm run build'"
    },

    "type": "module",
    "devDependencies": {
      "typescript": "^5.0.0",
      "esbuild": "^0.25.1",
      "nodemon": "^3.0.0"
    }
}
  