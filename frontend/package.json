{
    "name": "ft_transcendense",
    "version": "1.0.0",
    "scripts": {
      "build": "node build.mjs",
      "start": "node public/js/script.js",
      "hot-reload": "nodemon --watch src/ --exec 'npm run build'"
    },

    "type": "module",
    "devDependencies": {
      "typescript": "^4.0.0",
      "esbuild": "^0.14.0"
    }
  }
  