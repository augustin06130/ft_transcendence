version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./frontend/public:/var/www/server/frontend/public
      - ./backend:/var/www/server/backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
